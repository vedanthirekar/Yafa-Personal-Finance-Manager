# --- Frontend (Streamlit UI, calls the backend over REST via api_client.py) ---
streamlit
streamlit-audiorecorder
streamlit_extras
millify
requests

# --- Voice transcription: local Whisper model (backend/app/services/speech.py) ---
faster-whisper

# --- Legacy NLP pipeline, reused by backend/app/services/nlp_extract.py ---
pydub
nltk
word2number

# --- Shared data/plotting/ML ---
pandas
numpy
matplotlib
statsmodels
scikit-learn
PyYAML

# --- Backend service (FastAPI + REST API) ---
fastapi
uvicorn[standard]
pydantic[email]
pydantic-settings
sqlalchemy
bcrypt
python-jose[cryptography]
python-multipart

# --- Semantic categorization (BERT embeddings + Qdrant vector DB) ---
sentence-transformers
qdrant-client

# NOTE: torch is required by sentence-transformers but is intentionally NOT pinned
# here. Install the CPU-only wheel yourself first (much smaller/faster than the
# default CUDA wheel on a machine with no GPU):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# then run `pip install -r requirements.txt`.
